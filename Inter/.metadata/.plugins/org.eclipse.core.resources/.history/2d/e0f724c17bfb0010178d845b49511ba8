package problems;
import java.util.*;

public class Sample {
   public static void main(String[] args) {
      List<Integer> nums = new ArrayList<>();
      int[] a = { 3, 6, 7, 2, 9, 5, 8 };
      for (int i : a)
	 nums.add(i);
      List<Integer> filter = nums.stream().filter(ele -> (ele % 2 == 0)).toList();
      int[] arr = new int[filter.size()];
      for (int i = 0; i < filter.size(); i++) {
	 arr[i] = filter.get(i);
      }
      Arrays.sort(arr);
      for (int i : arr)
	 System.out.print(i + " ");
      
      String name = "diwakar";
      String name2 = "singh";
      
      System.out.println(name.length());
      System.out.println(arr.length);
      System.out.println(filter.size());
      
      // for(Integer i : filter)
      // System.out.print(i+" ");
      
      // employee eid, ename, salary;
      // department name, eId;
      // 1. SELECT ename, MAX(salary), dname FROM employee e INNER JOIN department
      // dept ON e.eid ==dept.eid
      // GROUP BY dname,ename;
      // 2. SELECT ename, MIN(salary), dname FROM employee e
      //
      // 3. SELECT * FROM employee INNER JOIN departmen USING(eid)
   }
}
